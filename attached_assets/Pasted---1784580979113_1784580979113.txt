ВНИМАНИЕ.

Ты несколько раз исправлял проект по одной ошибке, из-за чего каждый новый деплой находил следующую ошибку. Это неправильная стратегия.

ТВОЯ ЗАДАЧА — НЕ ИСПРАВИТЬ ОДНУ ОШИБКУ.

ТВОЯ ЗАДАЧА — ВЕРНУТЬ ПРОЕКТ В ПОЛНОСТЬЮ РАБОЧЕЕ СОСТОЯНИЕ.

==========================
ПРАВИЛА
==========================

1. Запрещено делать commit до полностью успешной production-сборки.

2. Запрещено делать push до полностью успешной production-сборки.

3. Запрещено исправлять ошибки по одной.

4. Нужно сначала найти ВСЕ несовместимости во всём auth-service, затем исправить их одним проходом.

==========================
ПРОАНАЛИЗИРУЙ ВЕСЬ ПРОЕКТ
==========================

Полностью проверь весь auth-service.

Проверь ВСЕ API routes.

Проверь ВСЕ Prisma-запросы.

Проверь ВСЕ сервисы.

Проверь ВСЕ util.

Проверь middleware.

Проверь helpers.

Проверь всё, что использует prisma.user.

Особенно найди:

projectId_email

findUnique

UserWhereUniqueInput

@@unique

findFirst

findMany

findUniqueOrThrow

==========================
ГЛАВНАЯ ЦЕЛЬ
==========================

После prisma db pull схема изменилась.

Если исчез уникальный индекс,

НЕ ИСПРАВЛЯЙ ОДИН ФАЙЛ.

Исправь ВСЕ места проекта, которые используют старую схему.

Никаких частичных исправлений.

==========================
ЧТО ЗАПРЕЩЕНО
==========================

Запрещено менять бизнес-логику.

Запрещено менять регистрацию.

Запрещено менять авторизацию.

Запрещено менять JWT.

Запрещено менять refresh token.

Запрещено менять verify email.

Запрещено менять reset password.

Запрещено менять forgot password.

Запрещено менять API.

Разрешается менять ТОЛЬКО обращения Prisma, несовместимые с текущей schema.prisma.

==========================
ПОСЛЕ ИСПРАВЛЕНИЙ
==========================

Запусти production-сборку.

npm run build

Если есть ХОТЬ ОДНА ошибка —

НЕ ДЕЛАЙ COMMIT.

Исправь следующую причину.

Повтори build.

Делай это до тех пор, пока build полностью не станет зелёным.

==========================
ПОСЛЕ УСПЕШНОЙ СБОРКИ
==========================

Ещё раз выполни глобальный поиск по проекту.

Если осталось хотя бы одно использование несовместимого Prisma API —

НЕ ДЕЛАЙ COMMIT.

Исправь.

Повтори build.

==========================
COMMIT
==========================

Commit разрешён только если одновременно выполнены ВСЕ условия:

✓ npm run build полностью успешен

✓ TypeScript без ошибок

✓ Prisma без ошибок

✓ Next.js без ошибок

✓ Production build успешен

✓ Нет несовместимых Prisma-запросов

Только после этого сделай один commit и один push.

==========================
ОТЧЁТ
==========================

Верни:

1. список всех изменённых файлов;

2. причину изменения каждого файла;

3. подтверждение успешного npm run build;

4. commit SHA.

Если не можешь выполнить все пункты — НЕ ДЕЛАЙ COMMIT, а остановись и объясни точную причину.