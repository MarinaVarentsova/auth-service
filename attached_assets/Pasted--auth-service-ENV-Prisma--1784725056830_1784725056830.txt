Работаем только в auth-service.

Палату не менять.
ENV не менять.
Домены не менять.
Prisma и БД не менять.
Ничего не коммитить до получения результата аудита.

ФАКТЫ

1. Восстановление пароля работает:
- письмо через UniSender отправляется;
- письмо приходит;
- ссылка работает.

2. Регистрация доходит до отправки письма и возвращает HTTP 502:

{
  "message": "Не удалось отправить письмо подтверждения. Попробуйте зарегистрироваться ещё раз.",
  "buildMarker": "auth-service-v3"
}

ЗАДАЧА

Сравнить рабочую отправку письма в forgot-password с нерабочей отправкой письма подтверждения в register.

Проверить:

1. Какую функцию отправки использует forgot-password.
2. Какую функцию отправки использует register.
3. Какие параметры передаются в UniSender в обоих случаях:
   - api_key;
   - sender_name;
   - sender_email;
   - recipient;
   - subject;
   - html/body;
   - list_id;
   - format;
   - Content-Type;
   - URL API.

4. Как формируется ссылка подтверждения регистрации:
   - какая ENV используется;
   - какой итоговый URL получается.

5. Добавить только временные безопасные логи:

[REGISTER VERIFY URL] — итоговая ссылка без полного токена;
[REGISTER MAIL STATUS] — HTTP status;
[REGISTER MAIL RESPONSE] — JSON ответа UniSender;
[FORGOT MAIL STATUS] — HTTP status;
[FORGOT MAIL RESPONSE] — JSON ответа UniSender.

Не логировать API key, пароль и полный токен.

6. Выполнить один тест forgot-password и один тест register с новым уникальным email.

7. Показать точную разницу между рабочим и нерабочим запросами.

НА ВЫХОДЕ НУЖНО ВЕРНУТЬ ТОЛЬКО:

1. Файл и функцию рабочей отправки forgot-password.
2. Файл и функцию нерабочей отправки register.
3. Итоговую verification URL.
4. HTTP status и JSON UniSender для forgot-password.
5. HTTP status и JSON UniSender для register.
6. Точную подтверждённую причину 502.
7. Минимальное исправление в 1–2 файлах.

После аудита не вносить исправление и не делать commit. Сначала вернуть мне результаты.